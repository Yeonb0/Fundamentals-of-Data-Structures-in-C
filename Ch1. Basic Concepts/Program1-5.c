// 프로그램 1.5: 정렬된 리스트 탐색
/*
while (there are more integers to check){
  middle = (left + right) / 2;
  if (searchnum < list[middle])
    right = middle - 1;
  else if (searchnum > list[middle])
    left = middle + 1;
  else
    return middle;
}
*/