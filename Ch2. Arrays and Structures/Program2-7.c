#define MAX_TERMS 101
// 프로그램 2.7 : 새로운 항을 첨가하는 함수
/*
void attach(float coefficient, int exponent){
  // 새로운 항을 다항식에 첨가한다
  if(avail >= MAX_TERMS){
    fprintf(stderr, "다항식에 항이 너무 많다.\n");
    exit(1);
  }
  terms[avail].coef = coefficient;
  terms[avail++].expon = exponent;
}
*/